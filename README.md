# python-dev-container-base
A forkable repo with basic config for python, dev container, jupyter, etc

## Run Jupyter Lab
Fix the config path in `scripts/run_jupyter.sh` and run the script:
```bash
./scripts/run_jupyter.sh
```